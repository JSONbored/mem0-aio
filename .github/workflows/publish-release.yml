name: Publish Release / Mem0-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-publish-release.yml@aab4f9852ef6ead106f17d23d9400d627fd67dac
    permissions:
      actions: read
      contents: write
    with:
      release_name: Mem0-AIO
      component: ""
      workflow_selector: build.yml
    secrets: inherit
