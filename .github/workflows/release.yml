name: Prepare Release / Mem0-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@7bfda522056a626fe401b734fbdd97239c0955b2
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: Mem0-AIO
      component: ""
      component_label: ""
      previous_tag_command: latest-aio-tag
    secrets: inherit
